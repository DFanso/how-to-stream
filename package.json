{
  "name": "how-to-stream",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "generate:hls": "bun scripts/generate-hls.ts"
  },
  "dependencies": {
    "mime": "^4.0.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
